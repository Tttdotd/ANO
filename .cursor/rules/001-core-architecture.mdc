---
description: 全局核心架构规范，约定技术栈、项目结构，确保全项目技术一致性和可维护性
alwaysApply: true
---

# 全局核心架构规范 (Core Architecture Rules)

## 1. 技术栈 (Tech Stack)
- **JDK**：作为项目的基础 Java 运行环境。
- **Spring Boot**：现代 Java Web 应用的主流开发框架，提供自动配置和约定优于配置的开发体验。
- **MyBatis Plus**：简化数据库持久层开发的 ORM 框架，提升 CRUD 效率。
- **MySQL**：主流关系型数据库，作为数据存储核心。
- **H2**：轻量级内存数据库，常用于开发与测试。
- **Lombok**：用于简化 Java 类代码，减少样板代码编写。
- **MapStruct**：实现对象间属性映射的编译期代码生成工具。


## 2. 项目包结构 (Recommended Project Structure)
严格遵循单一职责原则（SRP），分层规范如下：
```plaintext
com.tdotd.ano
├── common                 // 公共模块：常量、工具类
│   ├── constant           // 项目常量管理
│   ├── exception          // 自定义异常定义
│   ├── result             // 统一响应 Result 包装类, 包括基本的响应结构（如状态码、消息、数据等）
│   └── advice             // 全局异常处理器 (GlobalExceptionHandler)
├── config                 // 配置类：MyBatis Plus, Redis, Security 等
├── controller             // 表现层：仅负责参数接收、校验、转换和路由, 仅负责请求路由、入参校验、DTO 向 Service 的透传，不允许实现业务逻辑。
├── service                // 业务逻辑层：核心业务流、状态机转换、跨表逻辑
│   └── impl               // 接口实现类
├── mapper                 // 持久层：MyBatis Plus 的 Mapper 接口, 数据库操作接口，仅定义持久化相关逻辑。
├── domain                 // 领域对象
│   ├── entity             // 数据库映射对象 (PO)
│   ├── dto                // 数据传输对象 (Request Body) - 使用 Record
│   └── vo                 // 视图对象 (Response Data) - 使用 Record
└── infrastructure         // 基础设施层：外部 API 调用、第三方 SDK 集成
```

## 3. 异常与校验处理

- **异常传播规范**  
  - 禁止在 Service 层使用 `try-catch` 人为捕捉并返回错误信息，所有校验和业务失败场景需统一抛出 `com.ano.common.exception.BusinessException` 及其子类，并由全局异常处理器（GlobalExceptionHandler）兜底。
  - 所有接口响应必须使用统一响应结构（见 core 规范），严禁返回空对象或 null。


## 4. 统一响应规范 (Unified API Response)
- 所有 Controller 层接口方法必须返回 `com.ano.common.result.Result<T>` 类型的响应对象，不允许返回裸数据或其他自定义格式。
- 统一响应对象应包含：状态码、消息、实际数据三个字段，禁止在 Controller 层返回 null 或空对象。

## 5. 命名规范 (Naming Conventions)
- **Java**：所有类与方法命名统一使用 `camelCase`（小驼峰），接口类名前不加 `I` 前缀。
- **MySQL**：所有数据库表、字段、索引、约束命名均使用 `snake_case`（下划线命名，全部小写）。
- **Service 层方法**：命名必须表达业务语义，避免简单无意义的动词。如用 `initiateIteration()` （明确业务动作）代替 `updateTask()` 等通用方法名。

> 本规范为本项目所有成员的强制遵循项，任何 PR 不得违反上述约束。
