---
description: ANO (Action-Note-Output) 核心业务思想与产品定位。
globs: ["**/*.java", "**/service/**/*.java"]
alwaysApply: true
---

# ANO 产品哲学与业务逻辑深度定义

## 1. 产品核心定位：从“消耗时间”到“积累价值”
**ANO** 不仅仅是一个 Todo List，它是一个**知识炼金系统**。其核心理念源于对“时间价值”的思考：行动（Action）是成本，思考（Note）是转化，产出（Output）才是资产。

- **产品愿景**: 拒绝无意义的碎片化任务，强制要求每一个行动都能沉淀为可回溯的知识资产。
- **核心比喻**: 每一个 Task 都是一颗种子，Note 是养料，Output 是果实，Archive 是入库。

## 2. 核心闭环与状态机 (State Machine)
在编写 Service 层逻辑时，必须严格遵守以下不可逆的状态流转协议：

| 状态码 | 状态语义 (State) | 触发行为 (Action) | 业务逻辑约束 |
| :--- | :--- | :--- | :--- |
| **0** | **Todo** | 初始化创建 | 任务的初步构想，尚未开始执行。 |
| **1** | **Doing** | 确认开启任务 | 用户点击“开始”，进入正式执行阶段。 |
| **2** | **Noted** | 保存/更新笔记 | **核心约束**：只有关联了 Note 内容，才能进入此状态。 |
| **3** | **Done** | 提交产出链接 | **核心约束**：必须存在外部 Output URL，证明行动已产生实际结果。 |
| **4** | **Archived** | 手动归档 | **终态**：只有达到 Done 状态后，才允许执行归档操作。 |



## 3. 版本迭代哲学：Evolution (进化)
本项目拒绝简单的“编辑/覆盖”，而是倡导“迭代”。
- **禁止逻辑**: 严禁在原有任务上直接大幅度修改目标。如果任务目标改变，必须通过 `initiateIteration` 接口开启新版本。
- **溯源逻辑**: 
    - 新任务必须保留 `parent_id` 指向旧任务。
    - `version` 字段自增。
    - 形成“知识演化链条”，用户可以回溯一个想法从 V1 到 Vn 的进化过程。

## 4. 面试级工程标准 (ByteDance Interview Standards)
Cursor 在生成代码时，必须体现出“大厂候选人”的自我修养：
- **防御性编程**: 在状态流转前，必须校验前置条件（例如：没有 Note 内容时，严禁将状态置为 2）。
- **精度意识**: ID 必须是 String，防止 JS 精度丢失。
- **性能意识**: 分页查询（即便目前全量展示，代码结构也要预留 Limit 逻辑）、索引字段查询意识。
- **语义化逻辑**: 
    - 避免使用 `updateStatus(int s)` 这种模糊方法。
    - 优先使用 `markAsDone(String taskId, String url)` 这种具备业务语义的方法。

## 5. 开发者心智模型 (Mental Model)
当你（AI）在处理代码时，请想象你正在为一位西安的研一学生（他正在准备大厂面试）构建他的职业生涯代表作。
- **代码注释**: 应当解释“为什么这么做”（业务意图），而不仅仅是“做了什么”。
- **异常反馈**: 报错信息应具备引导性（例如：“请先完成思考笔记，再提交产出链接”），而非冰冷的“参数错误”。